{
    "name": "person",
    "version": "0.1.0",
    "scripts": {
        "compile": "babel -d lib/ es/",
        "prepublish": "npm run compile"
    },
    "main": "lib/person.js",
    "dependencies": {
        "core-js": "^0.6.0"
    },
    "devDependencies": {
        "babel": "^4.6.0"
    }
}